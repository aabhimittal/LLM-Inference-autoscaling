fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
# HTTP client for the vLLM provider (also used by the FastAPI test client).
httpx>=0.27
# Distributed cost ledger. Only imported when LLM_LEDGER=redis.
redis>=5.0
# Optional: accurate token counting. The service falls back to a heuristic if
# tiktoken is not installed, so it is not strictly required.
# tiktoken>=0.6
